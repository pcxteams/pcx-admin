# Base URL of the NestJS API (server-side only). The Next.js rewrite in
# next.config.ts proxies /api/auth/* here, and src/lib/session.ts reads the
# session from it. The browser never sees this value.
API_URL=http://localhost:3000
